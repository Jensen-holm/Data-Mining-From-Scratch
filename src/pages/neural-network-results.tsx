import Head from 'next/head';
import NavBar from '@/components/NavBar';
import { useRouter } from 'next/router';

import NeuralNetworkForm from '@/components/NeuralNetwork/NeuralNetworkForm';
import Plot from '@/components/Plot';


export default function NeuralNetworkResults() {
    // get the results from the query
    const router = useRouter();
    const { plot, mse } = router.query;
    return (
        <>
            <Head>
                <title>Jensen Data Miner: Neural-Network;</title>
                <meta name="description" content="Generated by create next app" />
                <meta name="viewport" content="width=device-width, initial-scale=1" />
                <link rel="icon" href="/favicon.ico" />
            </Head>
            <div className="relative">
                <NavBar />
                <div className="flex flex-col items-center justify-center h-screen sm:pt-20">
                    <NeuralNetworkForm />
                    <div className="mt-8 text-center">
                        <h5>Mean Squared Error: {mse}</h5>
                        {plot && <Plot plotData={plot} />}
                    </div>
                </div>
            </div>
        </>
    )
}
